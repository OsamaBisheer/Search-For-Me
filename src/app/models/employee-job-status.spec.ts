import { EmployeeJobStatus } from './employee-job-status';

describe('EmployeeJobStatus', () => {
  it('should create an instance', () => {
    expect(new EmployeeJobStatus()).toBeTruthy();
  });
});
